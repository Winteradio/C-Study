#include <stdio.h>

int main()
{
	/*
		FILE : FILE의 정보를 지닌 구조체
		fopen( 주소 + 파일이름, 'w'(쓰기) or 'r'(읽기) or 'r+'(읽기+쓰기) or 'a'(덧붙이기))
		fgets( 받을 포인터, 버퍼개수, 파일 구조체 변수 )
		fgetc( 파일 구조체 변수 )
		fputs( 넣을 변수, 파일 구조체 변수 ); 
		fseek( 파일구조체 변수, 0, SEEK_SET(처음자리로)); 
		fclose( 파일 구조체 변수 )  
	*/
	
	return 0;
}
