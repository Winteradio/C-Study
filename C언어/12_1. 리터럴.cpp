#include <stdio.h>

int main()
{
	// 리터럴 ( literal ) : 소스 코드상에서 고정된 값 
	// C언어의 경우, 큰 따옴표("")로 묶인 것들을 문자열 리터럴
	// 실행 중, 절대로 변경되어선 안되는 값들
	// 오직 읽기만 가능한 곳 
	char* AAA = "Hello"; // 문자열 리터럴 
	char BBB[6] = "Hello"; // 그냥 문자열 배열 : 낱개 문자임 
	return 0;
 } 
